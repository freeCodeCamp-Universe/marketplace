# Plugin Name

Short description of what this plugin does and the problem it solves.

This plugin is Claude-native: it has no portable skills or agents and runs only inside Claude Code.

## Installation

```sh
claude --plugin-dir ./claude-native/plugin-name
```

## Hooks

Configure hooks in `.claude-plugin/plugin.json` and add `hooks/hooks.json` if needed.
